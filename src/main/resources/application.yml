server:
  port : 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/skillbox_blog?useSSL=false&serverTimezone=Europe/Moscow
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
